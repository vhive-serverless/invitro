apiVersion: v1
kind: PersistentVolume
metadata:
  name: poddpvc
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadOnlyMany
  hostPath:
    path: /fileshare/models--gpt2-large/